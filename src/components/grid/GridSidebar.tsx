import type { FC } from 'react';

interface GridSidebarProps { }

const GridSidebar: FC<GridSidebarProps> = ({ }) => {
  return (
    <div>
      GridSidebar
    </div>
  );
}

export default GridSidebar;
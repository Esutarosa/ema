import type { FC } from 'react';

import { Container, Section } from '@/components/layouts';

const DiscographyPage: FC = () => {
  return (
    <Section>
      <Container className='text-center max-w-xl text-foreground/70 h-screen flex items-center justify-center'>
        Oops... In the near future the site will develop and something will definitely appear here, but for now it’s empty. Sorry about that...
      </Container>
    </Section>
  );
}

export default DiscographyPage;